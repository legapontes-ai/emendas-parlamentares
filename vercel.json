{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "prisma generate && if [ -n \"$Emendas_POSTGRES_URL_NON_POOLING\" ] || [ -n \"$DIRECT_URL\" ]; then prisma db push --skip-generate --accept-data-loss && npx tsx prisma/seed.ts; fi && next build"
}
