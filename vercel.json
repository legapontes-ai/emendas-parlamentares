{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "prisma generate && if [ -n \"$DIRECT_URL\" ] || [ -n \"$Emendas_POSTGRES_URL_NON_POOLING\" ]; then prisma migrate deploy; fi && next build"
}
